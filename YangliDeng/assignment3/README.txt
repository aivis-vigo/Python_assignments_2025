This project is a simple Python program that allows a user to save and view daily tasks.
The tasks are stored in a text file along with the date and time.
The project demonstrates the use of libraries, modules, file handling, and a directory structure.